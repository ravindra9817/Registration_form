export  class User {
  constructor(
    public name: string,
    public hosteler: boolean,
    // tslint:disable-next-line:variable-name
    public roll_no: string,
    public barnch: string,
    public email: string,
    public year: string,
    public percentage: string,
    public attandance: string,
    // tslint:disable-next-line:variable-name
    public mobile_no: number,

  ) {}
}
